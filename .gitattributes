* text=auto

# Ensure shell scripts use LF to avoid exec errors in Linux containers
*.sh text eol=lf
rabbitmq/entrypoint.sh text eol=lf

# Dockerfiles should be LF
Dockerfile text eol=lf
rabbitmq/Dockerfile text eol=lf

# PowerShell scripts can stay CRLF for Windows
*.ps1 text eol=crlf
